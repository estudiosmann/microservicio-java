name: CI

on:

  push:

jobs:

  build:

    runs-on: ubuntu-latest

    steps:

      - uses: actions/checkout@v4

      - name: Hola soy mauricionavarrete
        run: |
          echo "Hola soy mauricionavarrete!!"
